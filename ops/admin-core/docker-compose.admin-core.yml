services:
  api:
    environment:
      ADMIN_ACCOUNT_EMAIL: ${ADMIN_ACCOUNT_EMAIL:?ADMIN_ACCOUNT_EMAIL is required}
      ADMIN_RECOVERY_EMAIL: ${ADMIN_RECOVERY_EMAIL:?ADMIN_RECOVERY_EMAIL is required}
      ADMIN_MFA_REQUIRED: ${ADMIN_MFA_REQUIRED:?ADMIN_MFA_REQUIRED is required}
      ADMIN_MFA_CHALLENGE_TTL_SECONDS: ${ADMIN_MFA_CHALLENGE_TTL_SECONDS:?ADMIN_MFA_CHALLENGE_TTL_SECONDS is required}
      ADMIN_MFA_ENROLLMENT_TTL_SECONDS: ${ADMIN_MFA_ENROLLMENT_TTL_SECONDS:?ADMIN_MFA_ENROLLMENT_TTL_SECONDS is required}
      ADMIN_STEP_UP_TTL_SECONDS: ${ADMIN_STEP_UP_TTL_SECONDS:?ADMIN_STEP_UP_TTL_SECONDS is required}
      ADMIN_STEP_UP_MAX_FAILED_ATTEMPTS: ${ADMIN_STEP_UP_MAX_FAILED_ATTEMPTS:?ADMIN_STEP_UP_MAX_FAILED_ATTEMPTS is required}
      ADMIN_STEP_UP_RATE_LIMIT_WINDOW_SECONDS: ${ADMIN_STEP_UP_RATE_LIMIT_WINDOW_SECONDS:?ADMIN_STEP_UP_RATE_LIMIT_WINDOW_SECONDS is required}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY is required}
      SMTP_HOST: ${SMTP_HOST:?SMTP_HOST is required}
      SMTP_PORT: ${SMTP_PORT:?SMTP_PORT is required}
      SMTP_SECURE: ${SMTP_SECURE:?SMTP_SECURE is required}
      SMTP_USER: ${SMTP_USER:?SMTP_USER is required}
      SMTP_PASSWORD: ${SMTP_PASSWORD:?SMTP_PASSWORD is required}
      SMTP_FROM: ${SMTP_FROM:?SMTP_FROM is required}
